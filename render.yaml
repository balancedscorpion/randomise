services:
  - type: web
    name: randomisation-api
    env: docker
    plan: free
    healthCheckPath: /health
    autoDeploy: true
    dockerCommand: sh -c "uvicorn app.api:app --host 0.0.0.0 --port $PORT --log-level info"

